{
  "name": "schema",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "packages/schema/src",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "implicitDependencies": ["core"],
  "targets": {
    "build": {
      "executor": "@nx/rollup:rollup",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "dependsOn": ["^build"],
      "options": {
        "main": "packages/schema/src/index.ts",
        "tsConfig": "packages/schema/tsconfig.lib.prod.json",
        "outputPath": "dist/packages/schema",
        "format": ["esm"],
        "rollupConfig": "packages/schema/rollup.config.mjs",
        "assets": [
          {
            "input": "packages/schema",
            "glob": "README.md",
            "output": "."
          },
          {
            "input": "packages/schema",
            "glob": "package.json",
            "output": "."
          },
          {
            "input": "packages/schema/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          }
        ],
        "deleteOutputPath": true,
        "buildLibsFromSource": true,
        "generatePackageJson": false
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "command": "vitest run",
        "cwd": "packages/schema"
      }
    },
    "test:watch": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest watch",
        "cwd": "packages/schema"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
