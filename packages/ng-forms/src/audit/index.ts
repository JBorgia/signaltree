/**
 * Audit tracking utilities for SignalTree ng-forms
 *
 * Tree-shakeable exports - only included if explicitly imported.
 *
 * @packageDocumentation
 */

export {
  createAuditTracker,
  createAuditCallback,
  type AuditEntry,
  type AuditMetadata,
  type AuditTrackerConfig,
} from './audit';
