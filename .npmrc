# Use public npm registry instead of corporate registry
registry=https://registry.npmjs.org/

# Clear proxy settings for public access
# proxy=
# https-proxy=

# Keep SSL disabled for compatibility
strict-ssl=false

# Configure for pnpm compatibility
auto-install-peers=true
shamefully-hoist=false

# GitHub repository configuration for npm releases
# This links the npm package to the GitHub repository
# Format: @owner:registry=https://npm.pkg.github.com
# Note: This is for GitHub Packages registry (alternative)
# @JBorgia:registry=https://npm.pkg.github.com

# npm publish configuration
# Link package to GitHub repository (shows up on npm package page)
# This is handled in package.json repository field, but can be reinforced here
access=public

# npm authentication token should be set via environment or local config
# For CI/CD, set NPM_TOKEN environment variable
# For local: npm login or add token to ~/.npmrc (not this file - it's in git!)

# Optional: Set default npm author
init-author-name=Jonathan D Borgia
init-author-email=jonathanborgia@gmail.com
init-author-url=https://github.com/JBorgia
init-license=MIT
