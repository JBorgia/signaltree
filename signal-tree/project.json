{
  "name": "signal-tree",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "signal-tree/src",
  "prefix": "lib",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/signal-tree"],
      "options": {
        "project": "signal-tree/ng-package.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "signal-tree/tsconfig.lib.prod.json"
        },
        "development": {
          "tsConfig": "signal-tree/tsconfig.lib.json"
        }
      },
      "defaultConfiguration": "production"
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "signal-tree/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
